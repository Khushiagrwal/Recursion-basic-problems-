// Write a program in C to calculate the sum of numbers from 1 to n using recursion.

#include<iostream>
using namespace std;
int sum(int n,int i)
{
    if(i==n+1)
        return 0;
    return i+sum(n,i+1);
}
int main()
{
    int n;
    cin>>n;
    cout<<"SUM  is ="<<sum(n,1);
}
